<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base App Theme -->
    <style name="FrameworkRoot.Theme" parent="Theme.AppCompat.Light" />
    <style name="FrameworkRoot.Widget" parent="Theme.AppCompat" />

    <!-- Parent Theme -->
    <style name="Theme" parent="FrameworkRoot.Theme" />

    <style name="Theme.ImgurSample.Base" parent="Theme">
        <item name="actionBarInsetStart">@dimen/keyline_1</item>
        <item name="homeAsUpIndicator">@drawable/ic_up</item>

        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="colorAccent">@color/theme_accent_1</item>

        <item name="android:textColorLink">@color/theme_accent_1</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_up</item>
    </style>

    <!-- Abbreviated App Theme -->
    <style name="Theme.ImgurSample" parent="Theme.ImgurSample.Base" />

    <!-- Action Bar / Header Bar -->
    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">?actionBarIconColor</item>
        <item name="colorControlHighlight">#3fff</item>
    </style>

    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="HeaderBar">
        <item name="android:background">?colorPrimary</item>
        <item name="android:elevation">@dimen/headerbar_elevation</item>
    </style>

    <!-- ##### ACTIVITY THEMES ################################### -->

    <!-- ImgurSampleActivity-->
    <style name="Theme.ImgurSample.Main" parent="Theme.ImgurSample" />

</resources>